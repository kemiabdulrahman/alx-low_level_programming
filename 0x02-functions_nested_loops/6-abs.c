#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @j: int type number
 * Return: absolute value of @j
 */
int _abs(int j)
{
if (j < 0)
{
return (j * -1);
}
else
{
return (j);
}
}
